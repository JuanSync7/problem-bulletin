# v2.11-WP12 — Docs cleanup: phantom citations + dev-secret audit + v1 mechanism confirmation

**Status:** ✅ Complete.
**Final pytest:** `1281 passed / 0 failed / 5 skipped / 14 xfailed` (baseline 1274 + 7 new in `tests/test_no_v1_deferred_mechanism_wp12.py`).

WP12 covers the three small docs/audit items left at the bottom of v2.10 (Buckets D1/D2/D3). All three resolved without touching production code.

---

## D1 — `AION_BULLETIN_TEST_DOCS.md` citations

### Reframe up front

The brief described `docs/AION_BULLETIN_TEST_DOCS.md` as a "phantom" document and instructed delete-or-revive. **It is not phantom — the document exists**, is 3,508 lines, and every section cited by the 10 test files lines up:

| Citation form | Where in `AION_BULLETIN_TEST_DOCS.md` |
| --- | --- |
| `— Foundation Layer: app/...` | `### Foundation Layer` at L116 |
| `§Authentication / app/auth/...` | `### Authentication Subsystem` at L684 (sub-sections at `app/auth/magic_link.py` L756, etc.) |
| `— Mock/Stub Interface Specifications` | `## Mock/Stub Interface Specifications` at L14 |
| `lines 1731–1876` (`test_notifications.py`) | `### Watch & Notification Pipeline` exactly at L1731 |

The "phantom contract" framing in `v2.10-wp04a-diagnosis.md` follow-up #2 and `v2.10-wp04b-diagnosis.md` follow-up #3 was wrong — those WPs evidently checked the section names against the *implementation* (which had refactored) rather than against the doc itself, which still carries the section headers verbatim.

Per the brief: take path (a) (delete-or-replace) unless ≥5 tests cite the doc with coherent, still-honored contracts. We have **10** tests citing the doc and the contracts in question (Foundation Layer, Authentication, Watch & Notification Pipeline, Mock/Stub Interface Specifications) are still load-bearing. **Conclusion: keep every citation as-is.**

### Audit table (G1)

All 10 citations confirmed live; no edits required.

| File:line | Citation | Target section in TEST_DOCS | Status |
| --- | --- | --- | --- |
| `tests/auth/test_magic_link.py:3` | `§Authentication / app/auth/magic_link.py` | L756 `#### app/auth/magic_link.py` | Resolved (citation valid; kept) |
| `tests/services/test_notifications.py:8` | `lines 1731–1876` | L1731 `### Watch & Notification Pipeline` | Resolved (line anchor valid; kept) |
| `tests/auth/test_dependencies.py:3` | `§Authentication / app/auth/dependencies.py` | inside Authentication block L684+ | Resolved |
| `tests/auth/test_oidc.py:3` | `§Authentication / app/auth/oidc.py` | inside Authentication block L684+ | Resolved |
| `tests/test_config.py:3` | `— Foundation Layer: app/config.py` | L116 `### Foundation Layer` | Resolved |
| `tests/test_enums.py:3` | `— Foundation Layer: app/enums.py` | L116 | Resolved |
| `tests/auth/test_jwt.py:3` | `§Authentication / app/auth/jwt.py` | L684+ | Resolved |
| `tests/conftest.py:3` | `— Mock/Stub Interface Specifications` | L14 `## Mock/Stub Interface Specifications` | Resolved |
| `tests/test_exceptions.py:3` | `— Foundation Layer: app/exceptions.py` | L116 | Resolved |
| `tests/test_schemas.py:3` | `— Foundation Layer: app/schemas.py` | L116 | Resolved |

(The three `.claude/lessons-learned/*.md` hits — wp04a/wp04b/ticketing-v2.10 — are historical record and excluded per the brief.)

**Net D1 hit count:** 10 test/code citations + 3 historical lessons-learned references. Disposition: 10 × Resolved-as-valid, 3 × historical (untouched).

### Follow-up for v2.12

The wp04a/b "phantom" annotations are still in the historical record. Not removable (the brief forbids editing historical diagnoses), but future debuggers reading those follow-ups should be redirected by this WP's table. A one-line "superseded by v2.11-WP12" pointer in `ticketing-v2.11.md` lessons-learned (added below) closes the loop.

---

## D2 — `.env.example` / docs dev-secret audit

v2.10-WP05 deleted six placeholder defaults from `app/config.py`, including the `JWT_SECRET = "dev-secret-change-me"` footgun. Audit sweep below covers every doc/config file outside `app/`. Per the brief, `app/config.py` was not touched.

### Audit table (G2)

| File:line | Citation | Disposition |
| --- | --- | --- |
| `docs/DESIGN_REF.md:1191` | `- JWT_SECRET=dev-secret-not-for-production` (in a podman-compose YAML block) | **Resolved.** Replaced with `JWT_SECRET=__set_me__   # placeholder — must be set to a real ≥32-char secret; never use this literal in any environment`. This was the only literal "dev-secret"-style footgun in the docs. |
| `.env.example` (full file) | Mixed placeholders: `changeme` (DB password), `replace-with-random-secret-key`, `replace-with-real-secret`, etc. | **Resolved.** Rewrote with a header banner declaring every value a PLACEHOLDER (not a safe default), pointed at `v2.10-wp05-diagnosis.md` for the fail-fast contract, and replaced the more-plausible-looking placeholders with `__set_me__` / `__set_me_to_a_random_secret_at_least_32_chars__` so a copy-paste of the file is guaranteed to fail Settings validation. `SMTP_PORT=587`, `APP_NAME`, `STORAGE_PATH`, `BASE_URL` (localhost) kept as-is — those are genuine optional knobs with safe defaults. |
| `docs/AION_BULLETIN_ENGINEERING_GUIDE.md:3088, 3789` | `POSTGRES_PASSWORD ... changeme ... must be overridden in production` (compose-docs table) | **Resolved-no-change.** Already annotated "must be overridden in production"; the failure mode here is operational (compose default), not Settings-validation, and the doc explicitly warns. |
| `docker-compose.dev.yml:23`, `podman-compose.yml:39` | `POSTGRES_PASSWORD: changeme` / `${POSTGRES_PASSWORD:-changeme}` | **Deferred.** These are dev-compose files where `changeme` is a documented placeholder; the engineering guide warns "must be overridden in production". The brief restricts D2 to docs ("`.env.example` / README / docs/**"), not compose files. No README at repo root. Not touching unless a v2.12 hardening WP wants to fail-fast at compose level. |
| `docs/AION_BULLETIN_DESIGN.md:973, 1000, 1004` | Code-block excerpts of `app/config.py` showing `JWT_SECRET: SecretStr = Field(..., description=...)` and the min-32 validator | **Resolved-no-change.** These show the design-time contract (required, min-length), not a placeholder default. Match production. |
| `docs/AION_BULLETIN_IMPLEMENTATION_DOCS.md:45, 427, 658` | `JWT_SECRET: SecretStr` (no default) declarations and HS256 implementation steps | **Resolved-no-change.** No placeholder default shown; matches `app/config.py`. |
| `docs/AION_BULLETIN_ENGINEERING_GUIDE.md:224, 244, 256, 577, 593, 612, 621, 815, 2956, 2974, 3696, 3924, 3983` | Various JWT_SECRET references — all describing it as `SecretStr` **required** with "no default — must be set" wording | **Resolved-no-change.** Every hit either says "required / must be set" or describes runtime behaviour. None advertise the deleted `dev-secret-change-me` value. |
| `docs/AION_BULLETIN_TEST_DOCS.md:128, 144, 157, 163, 182, 713, 744` | Test-spec references to JWT_SECRET (SecretStr wrapping, required-fields list, missing-field validation) | **Resolved-no-change.** Match production fail-fast contract. |
| `.env.test:5` | `JWT_SECRET=test-jwt-secret-at-least-32-chars-long` | **Resolved-no-change.** Test-only file; value is a documented test-fixture, not a "safe default". Matches `tests/conftest.py:36` which already carries a load-bearing annotation. |
| `.claude/lessons-learned/v2.10-wp05-diagnosis.md` (multiple lines) | Historical record of the deleted footgun | **Historical — untouched** per brief. |
| `.claude/lessons-learned/ticketing-v2.{5,6,7,9,10,11}.md` (various) | Historical references in WP retrospectives | **Historical — untouched** per brief. |

### Note on README

No `README.md` exists at the repo root (only `.pytest_cache/README.md`, which is pytest-generated and out of scope). Brief permitted skipping in this case; noted here for completeness.

**Net D2 hit count:** ~30 grep hits inspected. 2 × Resolved-with-edit (`docs/DESIGN_REF.md`, `.env.example`); 0 × Deferred for docs; 2 × Deferred for compose files (out of brief scope); the rest Resolved-no-change as they correctly describe the post-WP05 fail-fast contract.

---

## D3 — `_v1_deferred.py` mechanism confirmation

### Filesystem + AST checks (manual)

| Invariant | Check | Result |
| --- | --- | --- |
| `tests/_v1_deferred.py` does not exist | `ls tests/_v1_deferred.py` | `No such file or directory` ✅ |
| `tests/conftest.py` has no `pytest_collection_modifyitems` def | grep + AST walk | Only mentioned in two explanatory comment blocks (L5–8, L173–177) — no def ✅ |
| No sub-conftest references the deleted module | rglob over `tests/**/conftest.py` non-comment lines | Zero hits ✅ |
| Top-of-file comment in `tests/conftest.py` documents the deletion | L5–8 already present from v2.10-WP07 | ✅ |

### Regression test (G3)

Added `tests/test_no_v1_deferred_mechanism_wp12.py` (7 tests, all passing). Asserts:

1. `tests/_v1_deferred.py` absent.
2. `tests/conftest.py` does not define `pytest_collection_modifyitems` (AST walk, not just grep — catches renamed/aliased/async/nested-class definitions).
3. No conftest under `tests/` references the deleted module name in a non-comment line (explanatory comments in `tests/conftest.py` are explicitly permitted).
4. **Self-test:** the AST detector function is exercised against a synthetic-bad source string that DOES define the hook, plus a synthetic-good string that just comments about it — proving the detector is not silently green.
5. **Edge cases:** parametrized over `async def`, nested-class `def`, and an unrelated function name.

This is the same pattern used by `tests/test_create_app_factory_lint_wp09.py` and `tests/test_spa_catchall_hardening_wp11.py` — source-shape invariants enforced as pytest items.

### Policy statement (G5)

> **v2.11 deferral-mechanism policy.** The v1 bespoke deferral registry
> (`tests/_v1_deferred.py` + a `pytest_collection_modifyitems` hook that
> auto-skipped any ID listed there) was deleted in v2.10-WP07 and must
> not be revived. All per-test deferral after v2.10 uses plain
> `@pytest.mark.skip(reason=...)` or `@pytest.mark.xfail(reason=...,
> strict=True)` with an explicit reason. Source-shape regression pin:
> `tests/test_no_v1_deferred_mechanism_wp12.py`.

This statement is appended verbatim to `ticketing-v2.11.md` (see below).

---

## Files edited / added (full list)

**Edited (2):**
- `docs/DESIGN_REF.md` — line 1191, replaced `JWT_SECRET=dev-secret-not-for-production` with `__set_me__` placeholder + warning comment.
- `.env.example` — rewrote with placeholder-banner header; all non-trivial values converted to `__set_me__` form; safe optional defaults kept.

**Added (1):**
- `tests/test_no_v1_deferred_mechanism_wp12.py` — 7-test regression pin for the deleted v1 deferral mechanism (filesystem + AST + self-test).

**Not touched (per brief):**
- `app/config.py` (settings layer is done, v2.10-WP05).
- Any `.claude/lessons-learned/*.md` file from v2.10 (historical record).
- `docker-compose.dev.yml` / `podman-compose.yml` (`changeme` placeholders for dev-compose are out of D2 scope; engineering guide already warns).

---

## Goals status

| Goal | Status | Evidence |
| --- | --- | --- |
| G1 — D1 audit table with per-citation disposition | ✅ | 10 citations enumerated above; all Resolved-as-valid (doc was not phantom). |
| G2 — D2 audit table with per-hit disposition | ✅ | ~30 grep hits inspected; 2 fixed (`DESIGN_REF.md`, `.env.example`); rest correctly describe fail-fast contract or are out of brief scope. |
| G3 — D3 regression test exists and passes | ✅ | `tests/test_no_v1_deferred_mechanism_wp12.py` — 7 passed. |
| G4 — full suite ≥ 1274 + new, 0 failed | ✅ | `1281 passed / 0 failed / 5 skipped / 14 xfailed` (1274 + 7 new). |
| G5 — diagnosis covers all 3 audits + one-line policy for v2.11 lessons | ✅ | Sections above + appended policy block in `ticketing-v2.11.md`. |

---

## v2.11 follow-ups

1. **The wp04a/b "phantom citation" annotations are wrong-but-historical.**
   They are pinned in the lessons-learned record (`v2.10-wp04a-diagnosis.md`
   follow-up #2; `v2.10-wp04b-diagnosis.md` follow-up #3). A future
   debugger reading those follow-ups should be redirected via this WP's
   D1 audit table. The v2.11 lessons-learned append below carries that
   pointer.

2. **No README at repo root.** Multiple v2.10 WPs assumed one existed
   (D2 brief mentions auditing it). A skeleton README pointing at the
   engineering guide / spec / `.env.example` / dev-compose flow would
   close that gap if anyone ever onboards from a clean clone. Not in
   scope for WP12 (brief: "no new top-level docs unless justified").

3. **Compose files retain `changeme` as the dev-DB password.** Brief
   restricted D2 to docs, not compose. A future hardening WP could
   either (a) require `POSTGRES_PASSWORD` to be supplied via env (no
   `:-changeme` fallback in `podman-compose.yml`), or (b) keep the
   current shape and accept the engineering-guide warning as the
   contract.

4. **Source-shape lints have hit critical mass.** WP08 (patch-str /
   text-cast), WP09 (factory + env audit), WP10 (naming-convention +
   alembic), WP11 (MagicMock headers + SPA catch-all), WP12 (v1
   deferral mechanism) — five lints all live in `tests/test_*_wp*.py`
   and follow the same AST-walk pattern. A v2.12 consolidation could
   factor a `tests/helpers/source_lint.py` with shared
   `walk_python_files(root)` + `ast_find(tree, predicate)` helpers.
