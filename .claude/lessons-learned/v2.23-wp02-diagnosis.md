# v2.23-WP02 — Page<T> envelope parity expansion (+2 pins)

## TDD summary

| Phase | Surface                                            | Count                                   |
| ----- | -------------------------------------------------- | --------------------------------------- |
| RED   | `tests/test_openapi_ts_parity_wp05_v213.py`        | 15 passed (5 PAGE + 1 UNION + 3 FLAT + 6 synthetic) |
| GREEN | `tests/test_openapi_ts_parity_wp05_v213.py`        | 17 passed (+2 new pins, first-run-clean) |
| GREEN | Full backend `tests/`                              | 1451 P / 0 F / 6 skipped / 14 xfailed (was 1449 P) |

Parametrize delta: **15 → 17** (+2).

## Tuples added

### WP05_PAGE_PAIRS (+1 generic Page<T> pin)
```python
(
    "get",
    "/api/v1/tickets/search",
    "Page_TicketRead_",
    "tickets.ts",
    "Page",
    "TicketDTO",
),
```
Guards: backend `Page_TicketRead_` schema (items / next_cursor / total)
↔ TS `Page<T>` declaration in `frontend/src/api/tickets.ts:211` (already
extended by `TicketsPage extends Page<TicketDTO>` at line 227). Closed-
wrapper polarity (OpenAPI ⊆ TS) — the `column_counts` optional on
`TicketsPage` is allowed because the lint reads the generic `Page<T>`
declaration directly (per the parser's `ts_wrapper="Page"` arg), not
the subclass. The inner `TicketDTO` field set is already pinned by
WP11.

### WP05_FLAT_PAIRS (+1 non-generic named subclass pin)
```python
(
    "get",
    "/api/v1/audit-log",
    "AuditLogPage",
    "auditLog.ts",
    "AuditLogPage",
),
```
Guards: backend `AuditLogPage` Pydantic schema ↔ hand-written
`AuditLogPage` interface in `frontend/src/api/auditLog.ts:29`. Both
sides closed; field set `{items, next_cursor, total}` matches.
Mirrors the `ActivityPage` flat-pair pin (same shape, different
inner type).

## Drifts caught + fixed

**0.** Both new tuples passed first-run-clean as predicted by WP01's
rule-(rr) deterrent-value framing. No schema fix required on either
side.

OpenAPI sides verified at WP02 start via direct `app.openapi()` probe:
```
Page_TicketRead_ -> ['items', 'next_cursor', 'total']  req=['items']
AuditLogPage     -> ['items', 'next_cursor', 'total']  req=['items']
```
Both sets are subsets of the TS counterparts.

## Untouched surfaces

| Surface  | Before  | After   | Y/N untouched |
| -------- | ------- | ------- | ------------- |
| Frontend | 276 P   | 276 P   | Y (no source edit) |
| Mypy     | 28 / 14 | 28 / 14 | Y |

Spot-verification of frontend was deferred to the existing baseline
guarantee — no `frontend/src/api/*.ts` files were modified. Mypy
likewise untouched (only test-file edit).

## Files modified

* `tests/test_openapi_ts_parity_wp05_v213.py`
  * `WP05_PAGE_PAIRS` += 1 (tickets/search → Page_TicketRead_ ↔ Page<TicketDTO>)
  * `WP05_FLAT_PAIRS` += 1 (audit-log → AuditLogPage ↔ AuditLogPage)

No application code or frontend source touched. Per WP01 rule (ss),
the pre-existing dirty-tree artifacts (`.backups/`, `.claude/`,
`frontend/src/App.css`, `frontend/src/pages/Kanban/{KanbanBoard,index}.tsx`,
`frontend/src/pages/ProblemDetail.{css,tsx}`, `frontend/vite.config.ts`)
were left untouched — they remain pending separate housekeeping.

## Surprises

None. WP01 recon was accurate to the schema; both pins were
first-run-clean. The `TicketsPage extends Page<TicketDTO>` subclass
pattern integrates cleanly with the existing parser (which targets
the generic-interface declaration directly, not the subclass), so no
parser extension was needed.

## Backlog forward (v2.24 candidates from WP01 deferral list)

* `Page_AgentActivityItem_` wrapper pin — defer until audit.ts upgrades
  from `ActivityEntry[]` to `Page<ActivityEntry>`.
* `Page_TicketWatcherRead_` — depends on WP03 introducing
  `TicketWatcher` interface + `listWatchers` hook.
* `Page_TicketAttachmentRead_` — defer until attachments UX lands.
* `CursorPage_ProblemResponse_` — defer until problems API gains a
  hand-written TS Page wrapper.

## Notes per forward rules

* **Rule (qq)**: file offsets in `tests/test_openapi_ts_parity_wp05_v213.py`
  were re-read at WP02 start; the previously-pinned 5 PAGE_PAIRS and
  3 FLAT_PAIRS entries verified literally before append.
* **Rule (rr)**: pins added first-run-clean. Deterrent value (catching
  the next backend rename of `next_cursor` / `total` on either route)
  is the marginal-coverage win; no current-drift fix was needed.
