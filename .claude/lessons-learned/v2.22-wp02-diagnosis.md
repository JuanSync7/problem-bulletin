# v2.22-WP02 Diagnosis — `SprintService.list → list_all`

## Outcome
Mechanical rename completed cleanly. Pure preventative-hygiene shadow sweep, no behaviour change.

## RED baseline (pre-rename)
- Mypy: **28 errors / 14 files** (134 source files checked)
- Tests: **1444 passed / 0 failed / 6 skipped / 14 xfailed** (109.90s)
- Sprint `.list(` surface: 1 declaration + 1 caller
  - `app/services/sprints.py:52` — `async def list(`
  - `app/routes/sprints.py:49` — `await sprint_service.list(`
- Cross-repo audit (`sprint_service\.list\b|SprintService.*\.list\b` excluding `list_all`): 0 other hits — confirming WP01 recon (0 test callers, 0 self-calls).

## GREEN (post-rename)
- Mypy: **28 errors** (unchanged — latent shadow, no PIN delta as predicted)
- Tests: **1444 passed / 0 failed / 6 skipped / 14 xfailed** (105.83s)
- Sprint surface grep for `.list(`: **empty**
- PIN `tests/test_typecheck_lint_v219_wp02.py`: **6 passed / 1 skipped**

## Files modified
1. `app/services/sprints.py:52` — `async def list(` → `async def list_all(`
2. `app/routes/sprints.py:49` — `await sprint_service.list(` → `await sprint_service.list_all(`

## Allow-list edits
None required. Latent shadow with 0 keys to remove.

## Surprises
None. Exactly mirrored the v2.21 `ProjectService.list → list_all` precedent. WP01 recon was accurate — no hidden callers, no `self.list()` invocations to patch in lockstep, no test fixtures referencing the old name.

## Forward rules exercised
- (ll) shadow-of-builtin — `list` shadowed `builtins.list`; rename eliminates the shadow.
- (pp) latent shadow flagging — caught and remediated before it surfaced as a PIN regression.

## Notes for v2.22-WP03+
Pattern is now established for both `ProjectService` (v2.21) and `SprintService` (v2.22-WP02). If further service-level `list` shadows exist (e.g. `TeamService`, `LabelService`, etc.), they follow the same 2-line rename template with no PIN/test impact.
