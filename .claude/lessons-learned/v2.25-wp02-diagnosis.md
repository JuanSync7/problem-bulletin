# v2.25-WP02 — tsc-noEmit config + mechanical sweep

Executed all six fix categories from WP01 diagnosis (A, B, C, E, F, G).
Real bug (D) deferred to WP03 as planned.

## Per-category landing

| Cat | Commit SHA | tsc count after | Vitest |
| --- | --------- | --------------- | ------ |
| Baseline | (pre WP02) `8221a45` | **76** | 276 P |
| A — `@types/node` + tsconfig `types: ["node", "vite/client"]` | `07e4319` | **59** (-17) | 276 P |
| B — target/lib → ES2022 | `1a77bd0` | **57** (-2) | 276 P |
| C — unused `import React` sweep (48 files) | `b042db4` | **9** (-48) | 276 P |
| E + F + G — unused locals + fixture `version: 1` + line-shift allow-list fix | `0ec4a6a` | **1** (-8) | 276 P |

## Final state

- **tsc-noEmit: 1** (expected — the WP03 real bug at `frontend/src/pages/ProblemDetail.tsx:1052`: `MouseEventHandler` vs `Promise<void>` mismatch).
- **Vitest: 276 P / 0 F** (invariant held across every commit).
- **Backend pytest: 1453 P / 6 skipped / 14 xfailed** (unchanged).
- **Mypy: 28 errors / 14 files** (unchanged — WP02 PIN floor).
- **Git: clean working tree.**

## Surprises / notes

1. **WP01 estimated 44 React-unused errors; actual was 48.** All 48
   files matched the safe pattern (either pure default or
   `React, { named }`). The sweep script flagged zero `React.X` body
   refs, so all 48 lines could be either deleted or stripped down to
   named imports only. Net diff: 48 files, 22 insertions / 48
   deletions.
2. **Cat G was solved as a free side-effect of Cat A** — adding
   `"vite/client"` to `compilerOptions.types` cleared both
   `import.meta.env` TS2339 errors. The WP01 plan anticipated this
   as a possible no-op for G; confirmed.
3. **Cat C did NOT need to be its own carve-out for `MineTab.tsx`** —
   that file uses `React.useState` etc. throughout but was never in
   the unused-React list (so the script never touched it). The TS6133
   on MineTab line 56 was for the destructured `user`, not the React
   import.
4. **Line-shift caused a side-effect lint break.** Removing the
   4-line unused `armTotal` helper in `Search.tsx` shifted later line
   numbers, which broke the catch-block lint allow-list (entry pinned
   to line 354, now at 350). Fixed in-commit by updating the
   allow-list constant. No production code change.
5. **`npm i -D @types/node` lockfile delta** was small (one devDep
   add). No dependency tree shake. Lockfile committed with WP02a.

## Deferrals

None. All Cat A/B/C/E/F/G work landed cleanly. WP03 retains the single
expected residual (Cat D — ProblemDetail.tsx:1052 `MouseEventHandler`
shape mismatch) plus the new CI gate (`tests/test_frontend_tsc_lint_v225.py`).

## Files touched (summary)

- **Config**: `frontend/package.json`, `frontend/package-lock.json`,
  `frontend/tsconfig.json`.
- **Source sweep (Cat C, 48 files)**: see `b042db4` diffstat — primarily
  `frontend/src/pages/**` and `frontend/src/components/**`.
- **Cat E/F/G targeted edits (6 files)**:
  - `frontend/src/api/__tests__/ticketDto.test.ts`
  - `frontend/src/pages/Activity/MineTab.tsx`
  - `frontend/src/pages/Kanban/TicketDetailDrawer.tsx`
  - `frontend/src/pages/Kanban/__tests__/KanbanHorizontalScroll.test.tsx`
  - `frontend/src/pages/Search.tsx`
  - `frontend/src/pages/__tests__/catch_block_lint.test.ts` (allow-list line shift)

## Sweep script (preserved for audit)

`/tmp/sweep-react.js` — Node script that:
1. Loads unique file list from `/tmp/react-unused-files.txt` (built
   from `npx tsc --noEmit | grep "'React' is declared"`).
2. For each file: locates the matching `import React[,…]` line.
3. Scans the rest of the file for `React.X` usage; skips if found.
4. Rewrites pure-default to deletion; rewrites mixed to named-only.

Ran clean: 48 modified, 0 skipped, 0 manual triage.

## WP03 hand-off

Per WP01 plan, WP03 needs to:
1. Write a Vitest regression test that asserts clicking the offending
   button at `ProblemDetail.tsx:1052` invokes `fetchData()` with no
   MouseEvent leakage (i.e., `showLoading` is undefined/true, not a
   MouseEvent object).
2. Apply the one-line fix: `onClick={() => fetchData()}`.
3. Add `tests/test_frontend_tsc_lint_v225.py` mirroring
   `tests/test_typecheck_lint_v219_wp02.py` structure with an empty
   allow-list and bidirectional drift detection. Skip cleanly if
   `frontend/node_modules` is absent.
4. Run the closure cycle (retrospective + v2.26 seed) per rule (yy).

Expected tsc-noEmit count after WP03: **0**, PINned by the new lint test.
