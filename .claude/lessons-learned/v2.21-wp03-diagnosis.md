# v2.21-WP03 Diagnosis — E1: `<Service>.list` shadow-of-builtin rename

## RED / GREEN

| Phase | Mypy errors | Backend tests |
|---|---|---|
| RED (entry)  | **43** errors in 17 files | 1444 P / 0 F / 6 skipped / 14 xfailed |
| GREEN (exit) | **28** errors in 14 files | 1444 P / 0 F / 6 skipped / 14 xfailed |

**Delta: −15 keys** — exactly matches the WP01 forecast.

## Renames performed

| Service | Old | New | Return shape |
|---|---|---|---|
| `TicketService` | `list` | `list_page` | `Page[Ticket]` |
| `ProjectService` | `list` | `list_all` | `list[Project]` |

## Files modified (6 total)

### Declaration sites (2)
- `app/services/tickets.py` — `list` → `list_page` (line 628) + internal self-call at line 1737 (recon miss; see Surprises)
- `app/services/projects.py` — `list` → `list_all` (line 127)

### Production callers (3)
- `app/routes/tickets.py:363` — `svc.list(...)` → `svc.list_page(...)`
- `app/routes/projects.py:86` — `project_service.list(...)` → `project_service.list_all(...)`
- `app/mcp_server/tools.py:154` — `svc.list(...)` → `svc.list_page(...)`

### Test callers (3 files; 8 sites)
- `tests/services/test_tickets_pagination.py:51`
- `tests/services/test_tickets_ordering.py:55,92,133,156,184`
- `tests/services/test_ticket_create.py:194,198`

(all `svc.list(` → `svc.list_page(` via uniform sed)

### Lint PIN (1)
- `tests/test_typecheck_lint_v219_wp02.py` — 15 entries removed from `_OFFENDER_ALLOWLIST`

## Allow-list keys removed (15, exact strings)

```
app/routes/projects.py:159:attr-defined
app/routes/tickets.py:258:attr-defined
app/routes/tickets.py:577:attr-defined
app/routes/tickets.py:605:attr-defined
app/routes/tickets.py:704:attr-defined
app/routes/tickets.py:705:attr-defined
app/routes/tickets.py:725:attr-defined
app/routes/tickets.py:798:attr-defined
app/services/projects.py:392:valid-type
app/services/tickets.py:1384:valid-type
app/services/tickets.py:1512:valid-type
app/services/tickets.py:1533:valid-type
app/services/tickets.py:1652:valid-type
app/services/tickets.py:1697:valid-type
app/services/tickets.py:1734:valid-type
```

All deleted via the forward-rule (jj) framework-limit-cleared pathway (BY-DESIGN, not LEGACY).

## Surprises

1. **One recon miss**: `app/services/tickets.py:1737` had an internal `self.list(...)` call inside `search_tickets`, not listed in WP01 production-callers table. Found via post-edit grep sweep and patched in lockstep. Forward rule (kk) — recon is approximate — applies. No test impact: it was a guard-branch self-delegation that the existing tests exercise transparently.
2. **`SprintService.list` exists** at `app/services/sprints.py:52` but does NOT currently own any mypy keys (Sprint service annotations don't return `list[Sprint]` in the same scope). It is the same shadow-of-builtin anti-pattern in latent form. Out-of-scope for WP03; noted for v2.22 hygiene seed.
3. **Key delta exact (-15)**: no double-cascading from sibling fixes; WP01 forecast was tight.

## Residual mypy state (28 errors / 28 allow-list keys)

The remaining 28 are the WP01-forecasted "framework residual floor": Starlette ASGI `Mount` callable variance, FastAPI `add_exception_handler` callable variance, SQLAlchemy `Mapped[T]` vs `T` boundary (multiple), lazy-loaded relationship `attr-defined`, `Result[Any].rowcount`, `feed.py:35` dict-item, `problems.py:164` return-value, `exceptions.py:33` name-defined under `TYPE_CHECKING`, `coalesce` assignment. All BY-DESIGN — would require SQLAlchemy mypy plugin or Starlette stub updates upstream to clear.

## Projected v2.21 closing state

- Mypy: **28 errors / 28 keys / 0 LEGACY** (matches WP01 forecast floor)
- Backend: 1444 P / 0 F / 6 skipped / 14 xfailed (preserved)
- Frontend: 276 P / 0 F (untouched)

v2.21 reaches the genuine framework-residual mypy floor as hypothesised in WP01. WP04 (closure) should record this as the v2.21 terminal baseline and seed v2.22 with two candidate fronts: (a) upstream-dependent plugin/stub adoption (SQLAlchemy mypy plugin re-eval — last reviewed in v2.20-WP03), and (b) the latent `SprintService.list` rename as a preventative-hygiene WP.
