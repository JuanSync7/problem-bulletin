# v2.12-WP05 Diagnosis — Bare-FastAPI Migration (Cluster 2 of 4)

## Framing

Cluster 2 of the WP09 allow-list bake-off. Eight more legacy `FastAPI()`-builder
test files migrated to `tests.helpers.app_factory.build_test_app()` so each route
test boots through the real `create_app()` wiring (middleware, central
`AppError` handler, per-route `EXCEPTION_HANDLERS`, etc.).

End state: 8 files migrated, allow-list shrinks 23 → 15, lint green, full suite
stays at 1321 P / 0 F / 5 skipped / 14 xfailed (+0 net vs WP04 baseline).

## Per-file notes

### 1. `tests/routes/test_notifications_wp25.py` — none

Straight swap. `_build_app` reduced to an overrides dict (`get_db`, `get_actor`)
fed into `build_test_app(dependency_overrides=…)`. 3 tests pass.

### 2. `tests/routes/test_notifications_wp30.py` — none

Same pattern as wp25. 3 tests pass.

### 3. `tests/routes/test_page_t_adoption_wp06.py` — none

Two bare `FastAPI()` sites:
- `_build_openapi_app()` — was mounting agents/projects/sprints/tickets routers
  by hand to assert OpenAPI shape. Replaced with `build_test_app()`; OpenAPI
  lookups are path-keyed so the larger app is harmless. The 19 parametrized
  schema assertions still pass.
- `test_agents_activity_runtime_shape_is_page_t` — its inline bare app gained
  full middleware via `build_test_app()`. Runtime GET still returns the Page[T]
  envelope unchanged.

### 4. `tests/routes/test_people_search.py` — none

Direct migration. 8 tests pass. Notably the `test_unauthenticated_returns_401`
case still works: when no actor override is supplied, real `get_actor` runs
under bearer-auth middleware and returns 401, same as before. 

### 5. `tests/routes/test_projects_permissions.py` — none, but redundant wiring dropped

The bare rig was re-registering:
- All entries of `app.routes.tickets.EXCEPTION_HANDLERS` (these are already
  wired in `create_app()` at line 265 of `app/main.py`).
- A duplicate `PermissionDeniedError` handler that lambda-returned
  `JSONResponse(status=403)` — production already wires this at lines 188–192
  of `app/main.py` with the central envelope shape. The test's lambda returned
  `{"detail": ...}` while production returns `{"error": {...}}`; assertions in
  this file all check `status_code == 403` only, no body shape, so no
  regression. The richer envelope is a strict improvement.

10 tests pass.

### 6. `tests/routes/test_projects_wp37.py` — none

Identical redundant-handler cleanup as file 5. 3 tests pass.

### 7. `tests/routes/test_project_wip_limits.py` — none

Same as files 5/6 (drop `EXCEPTION_HANDLERS` and `PermissionDeniedError`
inline reps). 5 tests pass. The OCC-409 assertion at the end
(`body["error"]["code"] in (...)`) already expected the canonical envelope,
so it now hits the real handler with the same shape.

### 8. `tests/routes/test_realtime_token.py` — none

Direct migration. The WS tests use `TestClient` without the `with` context
(no lifespan), which is fine because the realtime-WS handler doesn't depend
on lifespan startup. 7 tests pass including the WS handshake / token-purpose
checks.

## Deferred / scope cut

None. No latent production bugs found. The redundant-inline-handler pattern
(files 5/6/7) showed up exactly as predicted by WP04 heuristic #3 ("drop
redundant inline `add_exception_handler` — `create_app()` already wires them").
No prod-side changes were needed.

## Risks confirmed / new risks

- **WP04 heuristic #3 ("redundant inline `add_exception_handler`") was the
  dominant pattern in cluster 2** — files 5/6/7 all open-coded the same
  `PermissionDeniedError` handler + ticket `EXCEPTION_HANDLERS` loop. Worth
  highlighting in the WP09 helper docstring once the migration is done.
- **No SPA catch-all shadow encountered.** None of the cluster-2 files added
  custom test-only routes after `build_test_app()`.
- **No log-handler ordering issues.** None of the cluster-2 files install a
  log capture buffer.

## Allow-list shrinkage

23 → 15. Eight legacy entries removed:

- `tests/routes/test_notifications_wp25.py`
- `tests/routes/test_notifications_wp30.py`
- `tests/routes/test_page_t_adoption_wp06.py`
- `tests/routes/test_people_search.py`
- `tests/routes/test_projects_permissions.py`
- `tests/routes/test_projects_wp37.py`
- `tests/routes/test_project_wip_limits.py`
- `tests/routes/test_realtime_token.py`

## Conftest env audit (G5)

Checked `tests/test_conftest_env_audit_wp09.py` — no cluster-2 file is
referenced. No follow-up needed there.

## Final tally

```
1321 passed, 5 skipped, 14 xfailed, 5 warnings in 83.48s
```

Net delta vs WP04 baseline (1321 / 0 / 5 / 14): **+0**. Pure migration, zero
test count change, zero new xfails, zero new skips.
