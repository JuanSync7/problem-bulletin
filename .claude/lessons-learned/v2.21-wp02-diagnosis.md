# v2.21-WP02 Diagnosis

E2 mechanical rewrite: `pg_insert(Model.__table__) → pg_insert(Model)`.

## v2.21-WP02

### RED (pre-edit)
- Mypy: **51 errors / 51 allow-list keys** ✅ matches WP01 baseline.
- Backend: 1444 P / 0 F / 6 skipped / 14 xfailed (verified WP01).
- Two test functions pre-checked against the rewrite for fidelity (see "Surprise" below).

### Edits
8 callsites — every `pg_insert(TicketNotification.__table__)` → `pg_insert(TicketNotification)`:

| # | File:line |
|---|---|
| 1 | `app/services/due_soon_scanner.py:184` |
| 2 | `app/services/ticket_notifications.py:134` |
| 3 | `app/services/ticket_notifications.py:258` |
| 4 | `app/services/ticket_notifications.py:357` |
| 5 | `app/services/ticket_notifications.py:515` |
| 6 | `app/services/ticket_notifications.py:602` |
| 7 | `app/services/ticket_notifications.py:703` |
| 8 | `app/services/ticket_notifications.py:803` |

No import additions required — `TicketNotification` was already imported in both files.

### GREEN (post-edit)
- Mypy: **43 errors / 43 allow-list keys** ✅ exactly `51 − 8 = 43`. No cascading attr-defined disappeared; no new errors introduced. Delta matches forecast.
- Backend: **1444 P / 0 F / 6 skipped / 14 xfailed** ✅ preserved (after surprise fix below).
- PIN lint (`tests/test_typecheck_lint_v219_wp02.py`): 6 passed, 1 skipped ✅.

### Allow-list keys removed (8)
Per forward rule (jj): rewrite reclassifies these as fixed (not framework-limit), so deleted outright (not LEGACY):

- `app/services/due_soon_scanner.py:184:arg-type`
- `app/services/ticket_notifications.py:134:arg-type`
- `app/services/ticket_notifications.py:258:arg-type`
- `app/services/ticket_notifications.py:357:arg-type`
- `app/services/ticket_notifications.py:515:arg-type`
- `app/services/ticket_notifications.py:602:arg-type`
- `app/services/ticket_notifications.py:703:arg-type`
- `app/services/ticket_notifications.py:803:arg-type`

### Surprise — test monkey-patch fidelity
WP01 recon stated: "no test-caller rewrites needed because tests go through services, not `pg_insert` directly." This was **incorrect**. Two tests monkey-patch `pg_insert` and dispatch on identity of the first positional arg:

- `tests/services/test_ticket_notifications_wp40.py:178` — `if table is TicketNotification.__table__:`
- `tests/services/test_watcher_notifications_wp41.py:196` — `if table is TicketNotification.__table__:`

After the rewrite, the production code passes `TicketNotification` (the mapped class) rather than `TicketNotification.__table__`, so the identity check missed → `boom` fell through to `real_pg_insert` → forced-failure path not exercised → test assertions about SAVEPOINT isolation failed.

**Fix**: extend the predicate to accept either form:
```python
if table is TicketNotification or table is TicketNotification.__table__:
```
This is a test-only refinement preserving the original intent (intercept the ticket-notification INSERT only). Two-line change across two test files. Falls within WP02 scope as a necessary consequence of the rewrite.

### Residual
- Mypy: **43 errors / 43 allow-list keys** (target ~30 framework-residual floor; E1/WP03 will land remaining 15 keys).
- Frontend: not touched.
- Backend pytest: 1444 P / 0 F preserved.

### Forward
- WP03 (E1 — `<Service>.list` rename, 15 keys, ~19 sites) ready to start on this baseline.
- WP04 closure after WP03.
