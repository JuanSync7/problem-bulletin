# v2.18-WP01 — Baseline Confirmation

**Date:** 2026-05-26
**Scope:** Confirm seed baselines hold green on clean tree before WP02 work.
**Touched production code:** None (baseline-only WP).

---

## 1. Backend baseline

Command:
```
cd /home/kok-shew-juan/problem-bulletin && .venv/bin/python -m pytest -q --tb=short
```

Result: **1438 passed, 0 failed, 5 skipped, 14 xfailed, 3 warnings in 110.53s**

| Metric   | Seed | Observed | Match |
|----------|------|----------|-------|
| Passed   | 1438 | 1438     | OK    |
| Failed   | 0    | 0        | OK    |
| Skipped  | 5    | 5        | OK    |
| xfailed  | 14   | 14       | OK    |

Baseline holds. (Note: the `python` symlink is not on PATH at the host level; the
repo `.venv/bin/python` is the canonical interpreter — Python 3.12 inside venv,
host system Python is 3.14.4.)

---

## 2. Frontend baseline

Command:
```
cd /home/kok-shew-juan/problem-bulletin/frontend && npm test -- --run
```

Result: **Test Files 40 passed (40) — Tests 269 passed (269)** in 10.22s.

| Metric   | Seed | Observed | Match |
|----------|------|----------|-------|
| Passed   | 269  | 269      | OK    |
| Failed   | 0    | 0        | OK    |
| Files    | —    | 40       | n/a   |

Baseline holds. One benign `act(...)` console warning from
`AgentActivityFeed.tsx` surfaces during ActivityPage tests — not a failure,
pre-existing, not in scope for v2.18.

---

## 3. v2.17 lint files — existence & sanity

Both lint guards exist and run as part of the suites above (i.e. they passed
implicitly with the 1438/269 totals — no per-file re-run needed):

- `tests/test_type_ignore_lint_wp03_v217.py` — 16242 bytes, mtime 2026-05-26 19:16
- `frontend/src/__tests__/ts_any_lint.test.ts` — 17783 bytes, mtime 2026-05-26 19:10

---

## 4. LEGACY allow-list entries (v2.18 deletion targets)

### TS allow-list (`frontend/src/__tests__/ts_any_lint.test.ts`)

All 6 expected entries grep-confirmed in `_OFFENDER_ALLOWLIST`:

| # | File                              | Line | Kind | Cluster | Tag    |
|---|-----------------------------------|------|------|---------|--------|
| 1 | frontend/src/pages/Leaderboard.tsx | 64   | any  | L1      | LEGACY |
| 2 | frontend/src/pages/Leaderboard.tsx | 65   | any  | L1      | LEGACY |
| 3 | frontend/src/pages/ProblemDetail.tsx | 530 | any | L2      | LEGACY |
| 4 | frontend/src/pages/ProblemDetail.tsx | 534 | any | L3      | LEGACY |
| 5 | frontend/src/pages/ProblemDetail.tsx | 1156 | any | L2 pair | LEGACY |
| 6 | frontend/src/pages/ProblemDetail.tsx | 1207 | any | L3 pair | LEGACY |

Justification text in each entry explicitly references "LEGACY" and "v2.18 sweep"
or "tighten together". These are the exact L1/L2/L3 deletion targets.

### Python allow-list (`tests/test_type_ignore_lint_wp03_v217.py`)

The 1 expected entry grep-confirmed in the allow-list dict:

| # | File                                | Line | Tag    |
|---|-------------------------------------|------|--------|
| 1 | app/routes/notifications_v1.py      | 177  | LEGACY |

Justification text: narrows `row.actor_type` (Optional[str]) into typed `kind`
enum slot; tightenable by SQL row typing or inline cast. This is the L4
deletion target.

---

## 5. Summary

- Backend P/F: **1438 / 0** — matches seed.
- Frontend P/F: **269 / 0** — matches seed.
- 6 TS LEGACY allow-list entries: **present** (L1: Leaderboard 64/65; L2: ProblemDetail 530+1156; L3: ProblemDetail 534+1207).
- 1 Python LEGACY allow-list entry: **present** (L4: notifications_v1.py:177).
- Drift: **none** vs seed baselines.
- Blockers for WP02: **none**.

Ready to proceed to v2.18-WP02.
