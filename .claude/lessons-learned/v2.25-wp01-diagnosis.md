# v2.25-WP01 — Baseline + tsc-noEmit reconnaissance

## Baseline triplet (clean, matches v2.24 closure)

| Channel  | Result                                            |
| -------- | ------------------------------------------------- |
| Backend  | **1453 P / 0 F / 6 skipped / 14 xfailed** ✅       |
| Frontend | **276 P / 0 F** (41 test files) ✅                |
| Mypy     | **28 errors / 14 files** ✅ (matches WP02 PIN floor) |
| Git      | **0 dirty** ✅                                     |

## tsc --noEmit inventory

- Captured to `/tmp/tsc-errors.txt` (78 wc-lines).
- **Actual error count: 76** (78 lines − 2 continuation lines for the
  single TS2322 at `ProblemDetail.tsx:1052`, which spans 3 output
  lines).
- v2.24 rule (qq) stated "76 total / 23 in pages/__tests__/*". **Confirmed.**
  - `grep -c '^src/pages/__tests__/' /tmp/tsc-errors.txt` = 23.

### By TS error code

| Code   | Count | Meaning                                                   |
| ------ | ----- | --------------------------------------------------------- |
| TS6133 | 52    | `'X' is declared but never read` (unused locals)          |
| TS2304 | 11    | `Cannot find name 'global' / '__dirname'`                 |
| TS2741 | 4     | Missing property `version` on `TicketDTO` test fixtures   |
| TS2307 | 4     | `Cannot find module 'node:fs' / 'node:path'`              |
| TS2550 | 2     | `Property 'at' does not exist` — needs `lib: es2022`      |
| TS2339 | 2     | `Property 'env' does not exist on type 'ImportMeta'`      |
| TS2322 | 1     | Real bug — `MouseEventHandler` mismatch (ProblemDetail.tsx:1052) |

### Root-cause categorization (the actionable view)

| Cat | Root cause                              | Count | Codes                  | Fix complexity                                                                                |
| --- | --------------------------------------- | ----- | ---------------------- | --------------------------------------------------------------------------------------------- |
| **A** | Missing `@types/node`                 | **17** | TS2304 (11) + TS2307 (4) + … | **Config one-liner**: `npm i -D @types/node` + add `"node"` to `compilerOptions.types`. Clears `global`, `__dirname`, `node:fs`, `node:path` in one shot. |
| **B** | Missing `es2022` lib                  | **2**  | TS2550                 | **Config one-liner**: change `lib: ["ES2020", …]` → `lib: ["ES2022", …]` (and target → ES2022 for symmetry). |
| **C** | Unused `React` import (new JSX runtime) | **44** | TS6133 (subset)        | **Bulk edit**: tsconfig already has `jsx: "react-jsx"`, so `import React from 'react'` is genuinely unused. 41 unique files need the `import React` line removed (or changed to `import type` only). Mechanical sed-able. |
| **D** | Real type bug                         | **1**  | TS2322                 | **Single edit**: ProblemDetail.tsx:1052 — `onClick={fetchData}` where `fetchData: (showLoading?: boolean) => Promise<void>` collides with `MouseEventHandler`. Wrap in `() => fetchData()`. |
| **E** | Misc real-but-non-React unused locals  | **4**  | TS6133 (residual)      | **Single edits per file**: <br>• `Activity/MineTab.tsx:56` unused `user`<br>• `Kanban/TicketDetailDrawer.tsx:7` unused `listActivity`<br>• `Kanban/__tests__/KanbanHorizontalScroll.test.tsx:18` unused `screen`<br>• `Search.tsx:77` unused `armTotal` |
| **F** | Test-fixture missing `version` prop   | **4**  | TS2741                 | **Single file edit**: `ticketDto.test.ts` — add `version: 1` to four test fixtures. |
| **G** | `import.meta.env` typing              | **2**  | TS2339                 | **Config one-liner**: add `/// <reference types="vite/client" />` to a `vite-env.d.ts` (likely already present but not picked up — verify in WP02 whether it's missing from `include`). |

Sum check: 17 + 2 + 44 + 1 + 4 + 4 + 2 = **74**. Remaining 2 are residual
TS6133s already counted under (E). Net 76. ✅

## Fix-vs-PIN recommendation

Per project precedent (mypy WP02 PIN floor of 28, kept as allow-list
because the residuals are legitimate framework-friction): for tsc the
calculus is different — **the residuals are NOT framework-friction**,
they are all environment/config drift and one real bug. Recommendation
for every category is **FIX, do not PIN**:

| Cat | Decision | Reason                                                                            |
| --- | -------- | --------------------------------------------------------------------------------- |
| A   | **FIX in WP02** | Single devDep install + tsconfig types entry. Mechanical.              |
| B   | **FIX in WP02** | tsconfig one-liner. Mechanical.                                        |
| C   | **FIX in WP02** | 41-file mechanical sweep (delete unused `import React`).               |
| D   | **FIX in WP03** | Real bug, deserves a regression test (the existing PD tests don't cover button-click → `fetchData(event)` shape). |
| E   | **FIX in WP02** | 4 tiny single-line edits, no logic change.                             |
| F   | **FIX in WP02** | 4 test fixtures need `version: 1`. The DTO contract changed at WP14; tests didn't update. |
| G   | **FIX in WP02** | Likely `tsconfig.json`'s `include: ["src"]` already covers `src/vite-env.d.ts` — verify and possibly add `client.d.ts` reference. |

**Expected residual after WP02: ~1** (just category D).

## CI-gate recommendation

**YES — add the gate.** Per rule (kk), even a 0-residual floor drifts
without enforcement. Precedent: v2.19-WP02 `tests/test_typecheck_lint_v219_wp02.py`
runs mypy with an allow-list + bidirectional stale-entry detection.

Proposed: `tests/test_frontend_tsc_lint_v225.py` — runs
`npx tsc --noEmit` from `frontend/`, parses error output, compares
against an allow-list (initially empty after WP03), and fails on
bidirectional drift. The test must:

- Skip cleanly if `frontend/node_modules` is absent (CI envs without
  Node).
- Timeout-bound the subprocess (tsc on this codebase is ~12s, budget 60s).
- Use the same `path:line:errcode` keying as the mypy lint for
  consistency.

This gate is part of **WP03 deliverables**, alongside the (D) bug fix.

## WP02 / WP03 scope split

### WP02 — infrastructure sweep (Categories A, B, C, E, F, G)

1. `npm i -D @types/node` (single devDep add — package.json + lock).
2. Edit `tsconfig.json`:
   - `target: "ES2020"` → `"ES2022"`.
   - `lib: ["ES2020", "DOM", "DOM.Iterable"]` → `["ES2022", "DOM", "DOM.Iterable"]`.
   - Add `"types": ["node", "vite/client"]` to `compilerOptions`.
3. Mechanical sweep: remove `import React from 'react'` from 41 files
   where React is the only unused symbol. Use a small Node script or
   `grep -l "^import React from 'react'$"` + per-file Edit. Keep
   `import type { ... } from 'react'` lines untouched.
4. Four tiny single-line unused-local removals (category E).
5. Add `version: 1` to 4 ticketDto test fixtures (category F).
6. Verify `src/vite-env.d.ts` exists and is picked up; if missing,
   create it with `/// <reference types="vite/client" />`.
7. Expected tsc count after WP02: **1** (the ProblemDetail.tsx:1052 bug).

### WP03 — real bug + CI gate

1. **TDD the (D) bug**: write a Vitest test that asserts
   `<button onClick>` doesn't accidentally pass the MouseEvent into
   `fetchData`'s `showLoading` slot, or simply that clicking the
   button produces the same observable side-effect as
   `fetchData()`. Then fix line 1052 by wrapping: `onClick={() => fetchData()}`.
2. Add `tests/test_frontend_tsc_lint_v225.py` with empty allow-list
   and bidirectional drift detection (mirrors v2.19-WP02 structure).
3. Expected tsc count after WP03: **0**, PINned by the new lint test.

### WP04 — closure

Project may be ready for "both type channels at floor" closure post-WP03.
Mypy at 28 (PIN), tsc at 0 (PIN). v2.26 seed will need a new direction
(refactor backlog, perf, feature work) — no more known type-channel debt.

## Reference paths

- tsc raw output: `/tmp/tsc-errors.txt`
- tsconfig under audit: `/home/kok-shew-juan/problem-bulletin/frontend/tsconfig.json`
- Real bug site: `/home/kok-shew-juan/problem-bulletin/frontend/src/pages/ProblemDetail.tsx:1052`
- Precedent PIN test: `/home/kok-shew-juan/problem-bulletin/tests/test_typecheck_lint_v219_wp02.py`
